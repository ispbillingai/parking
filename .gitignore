# Local secrets / environment
config/config.php
.env
.env.*
!.env.example

# Composer
/vendor/
composer.phar

# XAMPP / server artefacts
*.log
*.tmp
*.bak

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*.swo
*~

# Claude local state
.claude/settings.local.json

# Cashmatic vendor binaries, certs, default credentials and docs
# (local-only reference material for integration — do not publish)
/reference/

# Build / cache
/cache/
/tmp/

# Runtime logs — keep the directory (storage/logs/.gitkeep), drop the files.
# The "*.log" rule above already excludes the log files themselves.
!/storage/logs/
